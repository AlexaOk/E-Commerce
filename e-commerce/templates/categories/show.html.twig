{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>

    <ul>
        {% for sub_categorie in sub_categories %}
            <li>{{ sub_categorie.name }}</li>
        {% endfor %}
    </ul>

    {#{% for product in products %}#}
        {#<div>#}
            {#<h3>{{ product.name }}</h3>#}
            {#<p>{{ product.description }}</p>#}
            {#<p>Categorie : {{ product.categorie.name }}</p>#}
            {#<p>Sous-catégorie : {{ product.sub_categorie.name }}</p>#}
            {#<p>{{ product.tags }}</p>#}
            {#<p>{{ product.date }}</p>#}
        {#</div>#}
    {#{% endfor %}#}
    {#<a href="{{ path('categories_index') }}">back to list</a>#}

    {#<a href="{{ path('categories_edit', {'id': category.id}) }}">edit</a>#}

    {#{{ include('categories/_delete_form.html.twig') }}#}
{% endblock %}