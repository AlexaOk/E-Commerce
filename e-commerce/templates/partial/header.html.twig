{#    <nav class="navbar navbar-dark bg-dark navbar-expand-lg">
<div class="container">
        <a class="navbar-brand" href="#">Tech-Factory</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">

                <li class="nav-item active">
                    <a class="nav-link" href="#">Home</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="#">Products</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="#">Cart</a>
                </li>
                
            
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item active">
                    <a class="nav-link" href="#">Admin Panel</a>
                </li>
                {% endif %}

                {% if app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="#">Logout</a>
                </li>
                {% else %}

                <li class="nav-item active">
                    <a class="nav-link" href="{{ path("users_registration")}}">Login/Sign up</a>
                </li>

                {% endif %}
                
            </ul>
            <form class="form-inline">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
</div>
    </nav>#}
    <header>

        <nav class="navbar navbar-inverse" style="margin-bottom: 0px; border-radius:0px; border:0px">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="#">LOGO</a>
                </div>

                <form class="navbar-form navbar-left" action="" style="margin-left: 30%;">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search">
                    </div>
                    <button type="submit" class="btn btn-default">Submit</button>
                </form>
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('users_registration') }}"><span class="glyphicon glyphicon-user"></span>Inscription</a></li>
                    <li><a href="{{ path('login') }}"><span class="glyphicon glyphicon-log-in"></span>Connexion</a></li>
                    <li>
                        <h4 id="my_margin">
                            <a href="#">
                                <span class="glyphicon glyphicon-shopping-cart">5</span>
                            </a>
                        </h4>
                    </li>
                </ul>
            </div>

            <!-- NAVBAR FOR THE CATEGORIE LOGIN CART ... -->

            <div class="container-fluid">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="#" id="active_acceuil">Accueil</a></li>
                    {% for categorie in categories %}
                        <li class="dropdown">
                            <a class="dropdown-toggle" data-toggle="dropdown" href="{{ path('categories_show', {'slug': categorie.name}) }}"><a href="{{ path('categories_show', {'slug': categorie.name}) }}">{{ categorie }}</a>
                                <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                {% for subCategorie in categorie.subCategories %}
                                    <li><a href="{{ path('sub_categories_show', {'category': categorie, 'sub_category':  subCategorie}) }}">{{ subCategorie }}</a></li>
                                {% endfor %}
                            </ul>
                        </li>
                    {% endfor %}

                </ul>
            </div>
        </nav>

    </header>